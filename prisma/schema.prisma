generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Reading {
  id        Int      @id @default(autoincrement())
  sensor_id String
  value     Float
  unit      String
  timestamp DateTime
}